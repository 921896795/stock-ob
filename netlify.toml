[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# 本地和生产统一用 /api/* 路径
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api"
  status = 200
